# B站视频下载器 —— 第三方依赖
#
# 安装：pip install -r requirements.txt
#
# 下面用的是 >= ，避免把依赖锁死、日后装不上。
# 如果你希望"今天能跑、明年也一模一样"，把 >= 改成 == 并填上当前版本。
# （版本号来源：本机已跑通的版本）

requests>=2.34.0          # HTTP 请求
lxml>=6.1.0               # 解析视频页，取标题和音视频流地址
moviepy>=2.2.0            # 合并分离的音视频轨
DrissionPage>=4.1.0       # 驱动浏览器，搜索取结果、登录取 Cookie
flask>=3.1.0              # 网页版界面
